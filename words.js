var Word = function(word) {
  
  this.word = word;

  this.length = word.length 


  // This method that creates a CastMember object from the constructor function we required and pushes it to the `this.cast` arr
};

// Exporting the Movie constructor which we will use in main.js
module.exports = Word;